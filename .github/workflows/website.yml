name: Website
on:
  push:
    branches: [main]
    paths: ['src/website/**', '.github/workflows/website.yml']
  pull_request:
    paths: ['src/website/**', '.github/workflows/website.yml']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - run: python src/website/build.py
      - run: python src/website/validate.py
      - run: |
          node --check src/website/site.js
          node --check src/website/theme.js
          node --check src/website/docs.js
          node --check src/website/protocol.js
